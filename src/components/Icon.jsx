import React, { useState } from "react";
import PropTypes from 'prop-types';

function Icon() {
    <div className="Icon">
        Icon
    </div>

    Icon.propTypes = {
        
    }
}

export default Icon;